# Example environment variables for the backend service

# Logging level (e.g., DEBUG, INFO, WARNING)
LOG_LEVEL=INFO

# Database connection string (placeholder for future integration)
DATABASE_URL=postgresql://user:password@host:5432/wildlife

# CORS allowed origins (comma-separated)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
